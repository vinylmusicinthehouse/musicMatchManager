# Overlap Explorer — v2025.08.18.1846.stable6g_hotfix5

### What's in this build
- Shadow-DOM override to restore **native** scrollbars and hide custom overlays on iOS/Android (fixes double-scrollbars).
- Native scrollbars are now visible on desktop Safari/Chrome (no longer suppressed).
- Prevent horizontal overflow in the component; keep momentum scrolling on mobile.
- Footer version text updated.


Minimal helpers only; loader untouched.