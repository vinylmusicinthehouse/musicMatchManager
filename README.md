# Overlap Explorer — v2025.08.18.1846.stable6g_hotfix5

### What's in this build
- Mobile/desktop scrollbar unification: remove custom overlay scrollbars, restore native scrollbars (fixes double-scroll on mobile and hidden scrollbars on desktop).
- Prevent horizontal overflow on narrow screens; improved single-column collapse around 920px.
- Minor HTML fix for `class="row2"` attribute typo to stabilize layout.

### Files
- `index.html` (root)
- `index_v2025.08.18.1846.stable6g_hotfix5.html` (versioned single-file copy)
- `VERSION.txt`
- `README.md`


Minimal helpers only; loader untouched.