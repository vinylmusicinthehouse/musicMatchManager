# Overlap Explorer — GitHub Pages Package
**Version:** v2025.08.18.0334.stable4

This build starts from your last working base and adds ONLY:
1) iOS-friendly overlay scrollbars on Overlap Explorer lists
2) Only-in-A / Only-in-B artist count badge (capsule) + small totals line
3) Export Shared Artists: combined count, per-side counts, Discogs artist URL when artist_id is present
4) Export Only A / Only B Artists: per-side counts + Discogs artist URL

Loader untouched. No MutationObservers. Exports compute on click.
